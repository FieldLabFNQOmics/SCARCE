% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vep_functions.R
\name{vep_config}
\alias{vep_config}
\title{Configure VEP}
\usage{
vep_config(
  mode = c("auto", "system", "conda", "singularity", "docker", "rest"),
  vep_path = NULL,
  conda_env = NULL,
  image = NULL,
  bind = NULL,
  cache_dir = Sys.getenv("VEP_CACHE", "~/.vep"),
  fasta = NULL,
  species = "homo_sapiens",
  assembly = "GRCh38",
  threads = max(1, parallel::detectCores() - 1L),
  perl_path = NULL,
  perl5lib = NULL,
  vep_env = NULL,
  extra_args = character(),
  perl_bin = NULL,
  perl_inc = character(),
  cache_version = NULL
)
}
\arguments{
\item{mode}{Execution backend for VEP. One of \code{"auto"}, \code{"system"}, \code{"conda"}, \code{"singularity"}, \code{"docker"}, or \code{"rest"}.
\code{"auto"} picks a sensible mode based on what is available; \code{"system"} calls a local VEP binary;
\code{"conda"} activates a conda env containing VEP; \code{"singularity"}/\code{"docker"} run VEP in a container;
\code{"rest"} uses the Ensembl REST API (local paths like \code{vep_path}, \code{cache_dir}, \code{fasta} are ignored).
Default: \code{"auto"}.}

\item{vep_path}{Path to the VEP executable or wrapper to call when \code{mode="system"} (e.g., \verb{/usr/bin/vep}).
Ignored for container/REST modes.}

\item{conda_env}{Name or path of the conda environment that provides VEP when \code{mode="conda"} (e.g., \code{"vep-114"} or
\code{"/path/to/envs/vep"}). Ignored otherwise.}

\item{image}{Container image to use when \code{mode} is \code{"singularity"} (e.g., \verb{/path/to/vep.sif}) or \code{"docker"}
(e.g., \code{"ensemblorg/ensembl-vep:release_114.0"}). Ignored for other modes.}

\item{bind}{One or more bind-mounts for container modes. For Singularity, a character vector of paths or
\code{"host:container"} mappings; for Docker, equivalent \verb{-v host:container} semantics. Use this to expose your
input/output/cache directories inside the container.}

\item{cache_dir}{Directory containing the VEP cache (e.g., \verb{~/.vep}). Used by all local modes; not used by \code{mode="rest"}.
Default comes from \code{Sys.getenv("VEP_CACHE")} or \verb{~/.vep}.}

\item{fasta}{Absolute path to the reference FASTA used by VEP (must match \code{species}/\code{assembly} and be indexed).
Optional for \code{mode="rest"}.}

\item{species}{Ensembl species identifier, e.g., \code{"homo_sapiens"}. Default: \code{"homo_sapiens"}.}

\item{assembly}{Genome assembly/build string, e.g., \code{"GRCh38"} or \code{"GRCh37"}. Default: \code{"GRCh38"}.}

\item{threads}{Number of CPU threads to request for VEP. Default: \code{max(1, parallel::detectCores() - 1L)}.}

\item{perl_path}{Optional character vector of directories to prepend to \code{PATH} before running VEP (helps locate \code{perl}
and VEP dependencies in non-standard installs).}

\item{perl5lib}{Optional character vector (or single string) to prepend to \code{PERL5LIB} for locating Perl modules.}

\item{vep_env}{Optional named list or named character vector of extra environment variables to set for the VEP run
(e.g., \code{c(HTTP_PROXY="http://...", PERL_MM_OPT="...")}).}

\item{extra_args}{Character vector of additional raw CLI flags to pass through to VEP (e.g., \code{c("--everything","--cache")}).}

\item{perl_bin}{Absolute path to the Perl interpreter to use (overrides whatever is found on \code{PATH}). Useful when VEP
requires a specific Perl. Optional.}

\item{perl_inc}{Character vector of include directories to add to Perl's \verb{@INC} (prepended for the VEP process).
Optional.}

\item{cache_version}{Ensembl cache version as a string (e.g., \code{"114"}). If supplied, may be used to validate or
construct cache paths/arguments; otherwise inferred by VEP where possible.}
}
\description{
Configure VEP
}
